# MLflow Configuration

# Tracking
tracking_uri: "./mlruns"  # Local directory for experiments
experiment_name: "brugada-ecg-classification"

# Artifacts
artifact_location: "./mlruns"

# Autologging
autolog: true
log_models: true
log_datasets: false

# Run Configuration
run_name_prefix: "brugada"
tags:
  project: "brugada-syndrome-detection"
  framework: "pytorch-lightning"
  task: "binary-classification"
