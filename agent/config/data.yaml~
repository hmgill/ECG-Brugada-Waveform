# Data Configuration for Brugada ECG Classification

# Paths
metadata_path: "metadata_updated.csv"
data_root: null  # Optional root directory for ECG files

# Data Splits
train_split: 0.7
val_split: 0.15
test_split: 0.15

# Cross-Validation
n_folds: 5
stratified: true

# Data Loading
batch_size: 32
num_workers: 4
pin_memory: true

# Preprocessing
normalize: true
normalization_method: "standardize"  # Options: standardize, minmax, per_lead

# Augmentation
augment_train: true
augment_val: false

# Augmentation Parameters
augmentation:
  amplitude_scale:
    range: [0.8, 1.2]
    prob: 0.5
  
  noise:
    std: 0.05
    prob: 0.5
  
  baseline_wander:
    amplitude: 0.1
    frequency_range: [0.1, 0.5]
    prob: 0.3
  
  time_warp:
    sigma: 0.2
    knots: 4
    prob: 0.2
  
  lead_scale:
    range: [0.9, 1.1]
    prob: 0.3

# Reproducibility
random_seed: 42
